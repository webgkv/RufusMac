# Screenshots

Drop UI screenshots here (e.g. `rufusmac.png`) and reference them from the main
README if useful.

For this Intel fork, a good hero shot is the main window on an Intel Mac under
macOS 14 or 15 (material fallback) and, optionally, macOS 26 (Liquid Glass).

This repository is a **best-effort** fork — see the root README **Scope &
maintenance** (support / further releases not guaranteed).
